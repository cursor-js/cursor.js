---
"@cursor.js/core": minor
---

Added `.if()`, `.do()`, and `.until()` methods for robust sub-queue flow control. This prevents deadlocks and enables automated UI synchronization inside chains (like waiting for carousels or checking input values before typing).
