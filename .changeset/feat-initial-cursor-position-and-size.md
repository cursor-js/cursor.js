---
"@cursor.js/core": minor
---

Added `setSize` and teleportation `move` methods to the `Cursor` class to allow programmatic scaling and absolute positioning. Updated `GhostCursor` to support `scale` via CSS transform.